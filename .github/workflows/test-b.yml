name: Repo B Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print message
        run: echo "hello manasi"
        
      - name: Wait for 20 seconds
        run: sleep 20
